# 全权限模式

AI 梗小游戏：在「全权限模式」下拦住开始 `rm -rf` 的 agent。

## 本地预览

直接用浏览器打开 `index.html`，或：

```bash
python3 -m http.server 8765
```

然后手机/桌面访问 `http://localhost:8765`。

## 操作

- 红色读条填满前，点 agent / 红色气泡 / 被盯上的文件
- 删掉 3 个文件出局
- 撑过 60 秒触发彩蛋

单文件、无外部依赖，适合发 X。
